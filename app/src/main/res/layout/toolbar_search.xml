<?xml version="1.0" encoding="utf-8"?>
<com.hexamass.votingknowledge.ui.custom.SearchToolbar xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/toolbar"
    style="@style/Toolbar"
    android:layout_width="match_parent"
    android:layout_height="?android:attr/actionBarSize"
    android:background="@color/colorPrimary"
    android:orientation="vertical"
    app:contentInsetLeft="0dp"
    app:contentInsetStart="0dp">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <ImageView
            android:id="@+id/back_button"
            android:layout_width="54dp"
            android:layout_height="match_parent"
            android:layout_centerVertical="true"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:padding="10dp"
            app:srcCompat="@drawable/ic_navigate_up" />

        <EditText
            android:id="@+id/search_edit_text"
            android:layout_width="match_parent"
            android:layout_height="36dp"
            android:layout_centerVertical="true"
            android:layout_toStartOf="@id/clear_button"
            android:layout_toEndOf="@id/back_button"
            android:paddingStart="12dp"
            android:paddingEnd="12dp"
            android:background="@drawable/bg_et_search"
            android:gravity="center_vertical"
            android:hint="Search Content ..."
            android:importantForAutofill="no"
            android:inputType="textNoSuggestions"
            android:maxLength="60"
            android:maxLines="1"
            android:minHeight="32dp"
            android:textSize="16sp"
            android:textColor="@color/grey_900"
            android:textColorHint="@color/grey_600"
            android:textCursorDrawable="@null"
            tools:ignore="UnusedAttribute" />

        <ImageView
            android:id="@+id/clear_button"
            android:layout_width="28dp"
            android:layout_height="28dp"
            android:layout_alignParentEnd="true"
            android:layout_centerVertical="true"
            android:layout_marginStart="8dp"
            android:layout_marginEnd="8dp"
            android:clickable="true"
            android:focusable="true"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:visibility="invisible"
            app:srcCompat="@drawable/ic_clear_grey" />

    </RelativeLayout>

</com.hexamass.votingknowledge.ui.custom.SearchToolbar>